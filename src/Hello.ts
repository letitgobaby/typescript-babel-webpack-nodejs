const test2 = 'what ? ';
const wellcomeWord = 'World!!';

const he = () => {
  console.log(`nope${  wellcomeWord}`);
  const check = true;
  console.log( check );
};

export { test2, he };

export default wellcomeWord;
